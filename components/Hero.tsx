import SliderHero from "./ui/SliderHero";

const Hero = () => {
  return (
    <section className="w-full max-h-[630px] overflow-hidden">
      <SliderHero />
    </section>
  );
};

export default Hero;
