import SwipperHero from "./ui/SwipperHero";

const Hero = () => {
  return (
    <section className="w-full max-h-[630px] overflow-hidden">
      <SwipperHero />
    </section>
  );
};

export default Hero;
